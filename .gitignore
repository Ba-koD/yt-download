/target
/downloads
/dist
*.part

# 작업 인수인계 메모. 로컬에서만 쓰고 저장소에는 올리지 않는다.
/HANDOFF.md

# 내려받은 도구는 빌드할 때 채운다
/tools/*/
!/tools/.gitkeep

# 앱이 남긴 임시/결과 파일
*.m4a
*.mp4
*.webm
*merge-*.txt

# 크롬이 확장을 읽으면서 만드는 파일. 우리 것이 아니라 담아 보내면 안 된다.
extension/_metadata/
