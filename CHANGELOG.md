# 변경 기록

이 파일은 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/) 형식을 따르고,
버전은 [유의적 버전](https://semver.org/lang/ko/)을 따릅니다.

버전 번호의 단일 출처는 `VERSION` 파일입니다. `scripts/release.ps1`(또는 `release.sh`)이
`VERSION`, `Cargo.toml`, 이 파일, git 태그를 한 번에 맞춰줍니다.

## [Unreleased]

### 고침

- 업데이트한 뒤 **다시 켜기가 앱을 죽이던 문제**. 방금 나간 프로세스가 쓰던 포트를
  윈도우가 2분쯤 붙들고 있어서(TIME_WAIT) 새로 뜬 앱이 그 자리를 잡지 못하는데,
  `YT_DOWNLOAD_ADDR` 로 주소를 지정해 두면 예비 포트로 넘어가지 않고 그대로 끝났다.
  갱신하고 나서 앱을 잃는 셈이었다. 이제 다시 켜지는 길에서는 지정한 주소여도
  빈 자리를 잡아 반드시 뜬다(앱 창은 자기가 잡은 주소를 열기 때문에 눈에는 같다).

## [0.3.0] - 2026-08-03

### 추가

- **자동 업데이트**. 앱과 확장 관리자가 스스로 최신 릴리스를 확인하고 갱신한다.
  릴리스 조회·SHA256 대조·압축 풀기·실행 파일 바꿔 끼우기는 공용 크레이트(`update/`)가
  맡고, 앱과 관리자는 "무엇을 받을지"만 정한다.
  - 다 받아서 검사한 **뒤에** 바꿔 끼운다. 도중에 실패하면 쓰던 실행 파일이 그대로 남는다.
  - 관리자 권한도, 도우미 프로세스(숨긴 PowerShell 같은 것)도 필요 없다.
    윈도우에서 돌고 있는 파일은 지울 수 없지만 이름은 바꿀 수 있다는 성질을 쓴다
    (`self-replace`).
  - macOS 는 `.app` 안의 실행 파일과 `Info.plist` 를 함께 바꾼다.
  - 앱은 화면 오른쪽 위 단추 하나로 확인 → 받기 → 다시 켜기를 밟는다.
    관리자는 확장과 자기 자신을 따로 갱신한다.
- **확장이 숏츠(`/shorts/`)에서도 된다.** 좋아요 위에 **구간** 버튼이 생기고, 누르면 영상
  옆에 편집 패널이 뜬다. 편을 넘기면 새 영상의 화질 목록으로 갈아 끼운다.
  - 버튼은 유튜브의 좋아요 칸과 똑같은 모양·간격으로 들어간다(48px 동그라미 + 글자, 여백 0).
  - 패널은 세로 버튼 줄 오른쪽 빈자리를 **재서** 세운다. 영상도 버튼도 가리지 않고,
    창이 좁으면 화면 아래로 눕는다. 제목 줄을 잡아 원하는 자리로 옮길 수도 있다.
- 확장에 **양 끝으로 보내기**를 넣었다. `⇤` `⇥` 버튼 또는 `Home` `End` 키로
  시작점을 맨 앞, 끝점을 맨 끝으로 보낸다(끝에 두면 "끝까지"라 라이브를 따라간다).

### 고침

- 확장에서 구간 손잡이를 끌 때 영상이 매 순간 따라가던 것을, **놓을 때 한 번만** 옮기도록 바꿨다.
  끄는 내내 되감기가 이어져 화면이 어지럽고 어디를 잡았는지도 보이지 않았다.
  재생 위치 막대를 끄는 것은 전과 같이 곧바로 따라간다.

### 변경

- 릴리스 자산을 **프로그램별로 나눴다**. 앱(`yt-download-<플랫폼>`)과
  관리자(`yt-download-manager-<플랫폼>`)가 따로 올라간다. 한 압축본에 같이 들어 있던
  시절에는 4MB 짜리 관리자만 고쳐도 167MB 를 다시 받아야 했다.
  버전은 그대로 함께 간다 — `VERSION` 하나가 앱·관리자·확장을 다 정한다.

## [0.2.0] - 2026-08-03

### 추가

- **크롬 확장(`extension/`, 실험 중)**. 유튜브 영상 페이지에 구간 편집 패널을 얹어
  고른 구간만 mp4 하나로 받는다. 데스크톱 앱 없이 브라우저 안에서 전부 처리한다.
  - 웹 클라이언트는 SABR 로 넘어가 주소를 주지 않으므로 `ANDROID_VR` 클라이언트로 물어본다.
    이 주소에는 속도 제한용 `n` 파라미터가 붙지 않아 서명 해독이 필요 없다.
  - 조각 색인(sidx) 2KB 만 받아 "시간 → 바이트"를 계산하고, 구간에 걸치는 조각만 받는다.
  - 영상·소리 트랙을 재인코딩 없이 mp4 하나로 엮는다(앞머리를 두 트랙짜리로 새로 쓰고
    조각을 시간 순서로 끼운다). 4K(AV1)와 1080p(H.264) 모두 확인했다.
  - 권한을 하나도 요구하지 않는다. content script 가 youtube.com 오리진에서 돌기 때문에
    InnerTube 는 동일 출처로, 미디어는 Range 를 허용하는 CORS 로 그대로 받는다.

- 브라우저 칸을 처음 켤 때 **이 컴퓨터의 기본 브라우저**로 채워준다(`Chrome (기본)`).
  Windows 는 레지스트리, Linux 는 `xdg-settings`, macOS 는 LaunchServices 에서 읽는다.
  한 번이라도 직접 고른 적이 있으면 그 선택을 덮어쓰지 않는다.
- 불러온 영상의 **정보 카드**. 미리보기 바로 아래에 제목·채널·올린 날짜·길이·화질·프레임·
  조회수·좋아요·공개 범위를 한눈에 보여준다.
- 내 영상 목록에 **제목 검색과 기간 거르기**. 최근 1주/1개월/3개월/1년 중에서 고르거나
  날짜를 직접 지정할 수 있고, 탭의 개수도 거른 결과에 맞춰 바뀐다.
  목록에 올린 날짜(`2일 전`)와 진행 중인 방송 표시(LIVE)도 함께 나온다.
- 확장 패널을 유튜브 페이지 안으로 옮겼다. 좋아요·공유 줄에 **구간 받기** 버튼이 생기고,
  누르면 영상 설명 아래에 편집 패널이 펼쳐진다. 화질 목록은 처음 열 때만 받아온다.
- 재생 버튼을 정리했다. **구간 재생**은 글자만 남기고(재생 중에는 `구간 정지`),
  재생과 일시정지는 상태에 따라 바뀌는 **버튼 하나**로 합쳤다.
- 구간 편집 화면을 다시 배치했다. 버튼을 **재생 / 지금 위치를 / 1초씩** 세 묶음으로 나누고
  이름과 구분선을 붙였다. 확대·축소는 타임라인 위에 붙여 하나의 덩어리로 만들었다.
  미리보기가 편집 영역을 밀어내 아래가 잘리던 것도 고쳤다.
- 구간 편집에 재생 버튼을 셋으로 나눴다. **구간**(IN에서 OUT까지만 재생하고 스스로 멈춤,
  재생 중에는 정지 버튼이 됨), **재생**(지금 위치에서 이어 재생), **일시정지**.

### 바뀜

- 릴리스 빌드에서 `codegen-units = 1` 을 뺐다. 빌드는 눈에 띄게 빨라지고(65초 → 49초)
  실행 파일은 0.5MB 늘어난다. 무거운 일은 yt-dlp 와 ffmpeg 가 하므로 이 설정으로
  얻는 실행 속도는 사실상 없다.

### 고침

- 로그인을 적용해도 내 영상 목록이 비어 있던 문제. 원인이 셋이었다.
  - 쿠키 파일에는 google.com·google.co.kr·youtube.com 것이 섞여 있는데 `SID` 같은 이름은
    도메인마다 값이 다르다. 전부 한 헤더로 이어붙여 보내는 바람에 유튜브가 로그아웃으로
    취급했다. 이제 보낼 호스트에 해당하는 쿠키만, 이름당 하나씩 보낸다.
  - 내 채널 ID를 절대 URL(`https://www.youtube.com/channel/…`)에서만 찾았는데 유튜브는
    상대 경로와 `browseId` 로만 내려준다. 실제로 나오는 형태를 모두 본다.
  - 구글에만 로그인하고 유튜브를 한 번도 열지 않으면 youtube.com 에 세션 쿠키가 생기지
    않는다. 이제 로그인 창을 유튜브로 보내 쿠키가 생긴 뒤에 내보내고, 그래도 없으면
    "적용 완료" 대신 무엇을 더 해야 하는지 알려준다.
- 내 영상 목록에 비공개·일부공개 영상이 빠져 있던 문제. 공개 탭 대신 업로드
  재생목록(`UU…`)에서 가져온다(어떤 채널에서는 131개 → 1389개).
- 로그인 창이 이미 떠 있으면 "앱 로그인 브라우저가 제시간에 시작되지 않았습니다"로
  끝나던 문제. 프로필을 이미 잡은 창이 있으면 그 창에 다시 붙는다.
- 목록을 읽을 때 자바스크립트 실행기(deno)를 넘기지 않던 경로가 있었다.
- 방송이 잦은 채널에서 동영상 칸이 두어 개만 차던 문제. 업로드 재생목록의 최근 200개가
  거의 스트림이라 동영상이 걸리지 않았다. 이제 업로드 목록과 채널 탭 셋을 함께 읽어
  합친다(같은 채널에서 2개 → 133개, 소요 시간은 4초 그대로).
- 도구 상태 칸에 `ffmpeg ffmpeg versi` 처럼 잘린 글자가 나오던 문제. 정식 버전이 아닌
  빌드는 빌드 번호(`N-125881`)까지만 보여준다.
- 아직 누를 수 없는 다운로드 버튼이 흐린 빨강이라 오류처럼 보이던 문제.
- `Could not copy Chrome cookie database` 가 나올 때 엉뚱한 해결책을 안내하던 문제.
  크로미움 계열은 실행 중에 쿠키 DB를 배타적으로 잠가서, 공유 플래그로도 열 수 없다.
  브라우저를 닫는 것 말고는 방법이 없으므로, 켜둔 채로 되는 앱 로그인을 먼저 안내한다.
- 확장이 미디어를 받을 때 `failed to fetch` 로 끝나던 문제. content script 에서 하는
  교차 출처 요청이 막힌다. 미디어 요청만 배경 일꾼이 대신 하도록 옮겼다.
- 그 뒤에 `HTTP 403` 과 `sidx 박스를 찾지 못했습니다` 가 이어지던 문제.
  배경 일꾼의 요청에는 `Origin: chrome-extension://…` 이 붙어 InnerTube 가 거절하고,
  `chrome.runtime.sendMessage` 는 JSON 직렬화라 받은 바이트가 통째로 사라진다.
  미디어는 페이지(MAIN) 쪽 작은 다리가 대신 받아오고 `postMessage` 로 넘긴다.
  배경 일꾼과 host_permissions 는 없앴다(권한 없는 확장으로 되돌아왔다).
- 확장이 **진행 중인 라이브**도 받는다. 라이브는 색인이 없어서 조각 번호(`&sq=N`)로
  하나씩 받아 엮는다. 조각마다 앞머리가 붙어 오므로 첫 조각의 것만 쓴다.
- 받은 파일에서 **소리가 앞서 가던 문제 두 가지.** 첫째, 소리 조각이 영상 조각보다
  늦게 시작하는 경우를 다루지 않아 그 차이만큼 소리가 앞섰다. 이제 소리를 영상이
  시작하는 지점부터 받는다. 둘째, 앞부분을 편집 목록(elst)으로
  건너뛰게 해뒀는데, 재생기가 그 지시를 소리에만 적용하고 영상에는 적용하지 않았다.
  이제 앞은 그대로 두고 뒤 길이만 맞춘다. 파일 이름은 실제 시작 시각으로 붙는다.
- **확장 관리자**를 별도 앱으로 추가했다. 설치·업데이트·삭제를 모두 맡는다.
  깃허브 릴리스를 보고 최신인지 확인한 뒤 받아서 정해진 자리에 풀어 놓는다. 크롬이 스토어 밖 확장을 자동으로 갱신해 주지 않아
  필요한 물건이다. 받은 파일은 릴리스에 적힌 SHA256 과 대조한다.
- 릴리스에 올라가는 확장 압축본에 `vendor/` 가 빠져 있었다. 그대로 나갔으면 로그인이
  필요한 영상이 조용히 실패했을 것이다. 이제 검사용 파일만 빼고 전부 들어갔는지 확인한다.
- 끝점을 맨 끝에 두면 시각 대신 **"끝까지"** 로 표시되고, 라이브는 방송이 나아가는 만큼
  끝점도 따라간다. 끝칸을 비워도 끝까지로 친다.
- 받는 동안 **일시정지·정지** 버튼이 생겼다. 이미 나간 요청은 끝내고 다음 조각부터 멈춘다.
- 확장이 **내 비공개·멤버 전용 영상**도 받는다. `ANDROID_VR` 은 로그인을 받아주지 않아
  창작자용 클라이언트로 다시 물어보는데, 쿠키만으로는 부족하고 유튜브 웹 화면과 같은
  `SAPISIDHASH` 인증 헤더가 있어야 한다. 인증을 통과해도 그 주소에는 속도 제한용
  `n` 이 붙어 있고 풀지 않으면 403 이라, yt-dlp 쪽 해결기(`vendor/`)를 들였다.
  해결기는 빈 iframe 안에서 돌린다. 유튜브 본 화면은 내장 함수가 바꿔치기돼 있어
  파서가 터지고, 해결기가 `location` 을 건드려 페이지가 날아가기 때문이다.
- 라이브 타임라인의 재생 위치 막대가 엉뚱한 곳에 놓이던 문제. 라이브에서는
  `video.seekable` 과 `duration` 이 실제 되감기 구간보다 한 시간쯤 앞을 가리킨다.
  이제 플레이어의 `getProgressState()` 를 그대로 받아 쓴다. 되감기 구간을 정확히
  알게 되면서, 라이브 전 구간 받기가 존재하지 않는 앞뒤를 긁던 것도 함께 고쳐진다.
- 받은 뒤 상태 줄에 **고른 구간과 길이**를 함께 보여준다. 조각 단위로 받느라
  파일이 몇 초 길어지면 그 길이도 같이 적는다.
- 고른 구간을 영상별로 기억한다. 다시 열면 지난번 구간이 되살아난다.
- googlevideo 가 다른 호스트로 넘길 때(`cms_redirect`) 넘어간 응답에 CORS 헤더가 없어
  간헐적으로 `failed to fetch` 가 나던 문제. 응답에 헤더를 붙이는 규칙을 넣고,
  규칙이 안 먹는 경우를 위해 배경 일꾼이 대신 받아오는 예비 통로를 뒀다.
  (규칙은 요청 대상과 요청을 시작한 쪽 **양쪽** 에 host 권한이 있어야 동작한다.)
- 라이브에서 구간이 `00:00 ~ 00:00` 으로 잡히던 문제. 진행 중인 라이브는 `duration` 이
  무한이라 길이를 알 수 없다. 되감기 가능한 구간(seekable)을 쓰고, 길이를 알게 되면
  손대기 전까지 전 구간을 따라간다.
- 타임라인을 누르기만 해도 영상이 맨 앞으로 튀던 문제. 길이를 모르는 동안에는 받지 않고,
  시간 칸을 고치는 것만으로는 재생 위치를 옮기지 않는다(손잡이를 끌 때만 따라간다).
- 확장 패널에 타임라인을 넣었다. 손잡이를 끌면 유튜브 영상이 그 지점으로 따라가서
  화면 자체가 미리보기가 된다. 시작·끝 찍기는 `[` `]` 버튼과 `I` `O` 단축키로 바꿨다.
- 확장이 유튜브에서 아무것도 못 그리던 문제. 유튜브가 Trusted Types 를 강제해서
  `innerHTML` 이 막힌다. 노드를 직접 만들어 붙이도록 바꿨다.
- 브라우저를 고르기만 해도 `--cookies-from-browser` 가 켜져서 쿠키 오류가 나던 문제.
  이 방식은 브라우저를 완전히 꺼야만 되므로, 이제 **직접 켤 때만** 쓴다
  (`브라우저 쿠키를 직접 읽기`). 평소에는 앱 로그인이 만든 쿠키 파일을 쓴다.
- 저장해둔 로그인이 만료됐을 때 내 영상 목록이 조용히 비던 문제.
  유튜브가 쿠키를 수시로 갈아치우기 때문인데, 이제 다시 로그인하라고 알려준다.
- `로그인 적용`이 `앱 로그인 열기`를 먼저 누르지 않으면 실패하던 문제.
  이제 창이 없으면 알아서 열고(이미 떠 있으면 그 창에 붙는다) 쿠키를 받아온다.

## [0.1.0] - 2026-08-02

첫 공개 버전.

### 추가

- YouTube 영상·라이브에서 원하는 구간만 골라 받는 데스크톱 앱(Rust + 시스템 webview).
- 진행 중인 라이브의 구간 다운로드. `&sq=N` 으로 DASH 조각을 직접 받아오고,
  HLS DVR 재생목록만 있는 경우에는 `sq × target` 으로 위치를 계산한다.
- 4K(2160p)까지 화질 선택. 라이브 메타데이터를 읽을 때 `--live-from-start` 를 붙여야
  고화질 포맷이 목록에 나온다.
- 타임라인 구간 편집기: 구간 길이에 맞춘 확대·축소, 선으로 표시되는 범위, 미리보기 연동.
- 미리보기는 영상의 실제 화면 비율을 따라가고 창 크기에 맞춰 커진다.
- 여러 연결로 나눠 받는 다운로드와, 영상·음성 합치기 진행률까지 하나로 이어지는 진행 표시.
- 로그 전용 콘솔 창(앱 창에서 "콘솔 창" 버튼).
- 비공개·연령 제한 영상을 위한 브라우저 쿠키 로그인.
- 받아둔 파일을 영상/쇼츠/라이브로 나눠 보여주는 보관함.
- 도구(yt-dlp, ffmpeg, ffprobe, deno)를 gzip 으로 압축해 실행 파일 안에 담는 포터블 빌드.
  파일 하나만 복사하면 되고 첫 실행 때 사용자 폴더로 풀어 쓴다.
- Windows 설치·제거 스크립트(`scripts/install.ps1`)와 Linux·macOS 설치 스크립트(`scripts/install.sh`).
- Windows·Linux·macOS(Apple Silicon/Intel) 빌드와 릴리스를 만드는 GitHub Actions 워크플로.

### 고침

- 라이브 구간 다운로드가 `This format cannot be partially downloaded` 로 실패하던 문제.
  `--live-from-start` 와 `--download-sections` 는 같이 쓸 수 없어서, 조각을 직접 받아오는 방식으로 바꿨다.
- 미리보기에서 고른 위치와 실제로 받아진 구간이 어긋나던 문제. 방송 시각 헤더
  (`X-Walltime-Ms` − `X-Head-Time-Millis`)에는 DASH 머리 지연이 섞여 있어 보정값이 매번 달라졌다.
  보정을 빼고 조각 번호만으로 위치를 잡으니 오차가 사라졌다.
- 작업을 취소해도 yt-dlp 자식 프로세스가 살아남아 멈춰 있던 문제(`taskkill /T`).
- 취소된 작업이 임시 파일을 남겨 디스크를 채우던 문제.
- 영상·음성 길이가 다를 때 긴 쪽 기준으로 잘리던 문제.
- 6시간짜리 영상을 열 때 화면이 몇 초간 비어 있던 문제. 메타데이터를 기다리지 않고
  미리보기를 먼저 띄운다.
- 포터블 빌드에 PATH 의 ffmpeg 를 복사해 담던 문제. 공유 라이브러리에 묶여 있어
  다른 PC 에서는 실행되지 않았다. 이제 세 플랫폼 모두 정적 빌드를 받아서 담는다.
- 도구를 바꿔도 다시 압축하지 않아 옛 도구가 그대로 담기던 문제. 수정 시각만
  비교했는데, 압축을 풀어서 받은 파일은 아카이브의 옛 시각을 물려받는다.
  이제 크기와 시각을 함께 확인한다.

[Unreleased]: https://github.com/Ba-koD/yt-download/compare/v0.3.0...HEAD
[0.3.0]: https://github.com/Ba-koD/yt-download/releases/tag/v0.3.0
[0.2.0]: https://github.com/Ba-koD/yt-download/releases/tag/v0.2.0
[0.1.0]: https://github.com/Ba-koD/yt-download/releases/tag/v0.1.0
